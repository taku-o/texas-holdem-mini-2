/kiro:spec-requirements "https://github.com/taku-o/texas-holdem-mini-2/issues/5 に対応してください。
作業用のgit branchを作ってから、要件定義書の作成に取りかかってください。
GitHub CLIが利用できます。"
think.

こちら修正してくれますか？
4件すべてが既存ファイルへの小規模な変更（1-3行の修正）で対応可能

/kiro:spec-tasks logic-bug-fixes

/simplify-loop

/kiro-review-spec logic-bug-fixes


これらの問題を修正してください。
  requirements.md

  軽微な問題（推奨改善）
  1. 一部の受け入れ基準がHOW（実装方法）を記述している — 要件2.1「重複除去後のランク値を降順にソートしてからストレート判定を行う」や要件3.1
  「ループ回数がプレイヤー数を超えた場合にループを終了する」は、期待される振る舞い（WHAT）ではなく修正手法（HOW）を記述している。WHAT例:
  「入力カードの順序に関係なくストレートを正しく検出する」「有限時間内に結果を返す」
  2. 導入セクションの見出し — 「プロジェクト説明 (入力)」はテンプレートの「Introduction」と異なる。機能上問題はないが統一性の観点。

  design.md

  軽微な問題（推奨改善）
  1. Error Handlingセクションが欠落 — テンプレートで定義されているError Handling戦略セクションが存在しない。純粋関数のバグ修正であり重大では
  ないが、getNextActivePlayerが-1を返す新しいエラーケースに関する伝播方針は本文中に記載があるとよい
  2. getNextActivePlayerの-1返却の安全性をresearch.mdに委譲 — 「呼び出し元の影響: 調査の結果、-1返却は既存フローで安全（research.md参照）」
  とあるが、設計書はself-containedであるべき（テンプレート指針）。要点を設計書内に記載する方がレビュアーに親切

/commit-commands:commit-push-pr
/review 13


どちらかの選択肢に倒して。良いと思われる側の選択にする。
  1. ギャップ分析の推奨とデザインの不一致（軽微）

  - gap-analysis.md L406-408: 推奨はオプションA（既存コンポーネント直接修正）
  - design.md: BUG-G4に対してdealCommunityCardsの純粋関数抽出（オプションB相当）を採用

これは修正して。
  3. タスク4の構造（軽微）

  tasks.md L653: - [ ] 4. バーンカード付きコミュニティカード配布を実装する がチェックボックス付きの親タスクとして存在するが、他のタスク（1,
  2, 3, 5）は親タスクなしの単一タスク。4.1と4.2のサブタスクがあるなら、タスク4自体のチェックボックスの扱い（いつチェックするのか）が不明確



修正して。
  3. design.md — getNextActivePlayerの-1返却安全性

  設計書内で「呼び出し元の影響」について記述がありますが、research.md
  の詳細な調査結果を設計書内にも要約として含めた方がself-containedになります。レビュー時の /kiro-review-spec
  でも同じ指摘が出ており、prompt.md にも修正依頼が記録されていますが、修正が反映されていないようです。

  4. tasks.md — タスク1, 2の (P) マーカーの意味が不明

  タスク1と2に (P) が付いていますが、タスク3以降にはありません。並列実行可能を示す？ドキュメント内に凡例がないため不明瞭です。

  5. design.md — エラーハンドリングセクション

  エラーハンドリングのテーブル（L301-305）は存在しますが、セクション見出しが ## エラーハンドリング
  ではなく内容が簡素です。dealCommunityCards のデッキ枚数不足ケースについて「通常のゲーム進行では発生しない」で片付けていますが、異常系（例:
   デッキが何らかの理由で消耗した場合）の振る舞いについて一言あると良いです。

  6. requirements.md — 受け入れ基準のWHAT vs HOW

  /kiro-review-spec で指摘された「受け入れ基準がHOW（実装方法）を記述している」問題について、prompt.md
  に修正依頼が記録されていますが、requirements.md 自体は修正されていません。具体的には:
  - 要件1.1: 「raiseAmountを0以上の値に制限する」→ 実装手法寄り
  - ただし、レビュー後の修正で要件2.1, 3.1は既にWHAT形式に修正済みのようです


この指摘は修正して
  新規指摘

  3. gap-analysis.md と design.md 間の軽微な不整合

  gap-analysis.md の推奨が「オプション A + BUG-G4のみオプション B」で、design.md
  もこの方針を採用している。整合は取れているが、gap-analysis.md L366の getNextActivePlayer の現状分析に以下の記述がある:

  ▎ currentIndexが自身もスキップ条件に合致する場合に自分のインデックスをそのまま返す

  これは現在の実装が「無限ループ」ではなく「不正な値（自分自身）を返す」問題であることを示唆している。一方で requirements.md と design.md
  は「無限ループ防止」をゴールとしている。ギャップ分析の詳細な分析結果が要件・設計の前提と若干ずれている可能性がある。ただし、ループ上限の追
  加と-1返却により両方のケースが解決されるため、実装上の問題にはならない。

  4. design.md — エラーハンドリングテーブル内の冗長性

  getNextActivePlayerの-1返却に関する記述が、エラーハンドリングテーブル（L314）とコンポーネント詳細セクション（L188-199の呼び出し元影響分析
  テーブル）の2箇所に重複している。内容は一貫しているが、将来の変更時に片方だけ更新される懸念がある。エラーハンドリングテーブルから詳細セク
  ションへの参照（「詳細はgetNextActivePlayerコンポーネント節の呼び出し元影響分析を参照」等）にすることで一元化できる。


