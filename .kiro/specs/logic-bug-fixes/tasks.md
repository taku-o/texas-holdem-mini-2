# 実装計画

- [ ] 1. (P) Raise金額の負値防止ガードを追加する
  - Raiseアクション処理で、ベット目標額が既にベット済みの額を下回る場合にRaise金額が負値にならないようガードを追加する
  - ガード追加後、プレイヤーのチップが不正に増加しないことを確認する
  - 目標額が既ベット額より小さいケースでRaise金額が0として処理されることを単体テストで検証する
  - チップ残高が減少のみで増加しないことを単体テストで検証する
  - _Requirements: 1.1, 1.2, 1.3_
  - _Contracts: applyAction Service_

- [ ] 2. (P) ストレート検出に明示的ソートを追加する
  - ハンド評価のストレート判定で、重複除去後のランク値を降順にソートしてからストレート検出を行うよう修正する
  - ソートされていないカード配列を入力してもストレートが正しく検出されることを単体テストで検証する
  - フラッシュカードのサブセットに対するストレートフラッシュ判定がカードの挿入順序に依存しないことを単体テストで検証する
  - _Requirements: 2.1, 2.2, 2.3_

- [ ] 3. 次のアクティブプレイヤー検索にループ上限を追加する
  - 次のアクティブプレイヤーを検索するループにカウンタを追加し、プレイヤー数を超えた場合にループを終了する
  - 全プレイヤーがフォールド済みまたはチップ0の異常状態で、例外を発生させずに-1を返すようにする
  - 正常な状態（アクティブプレイヤーが存在する）での動作が従来通りであることを単体テストで検証する
  - 全員が非アクティブの状態で-1を返すことを単体テストで検証する
  - _Requirements: 3.1, 3.2, 3.3_
  - _Contracts: getNextActivePlayer Service_

- [ ] 4. バーンカード付きコミュニティカード配布を実装する
- [ ] 4.1 バーンカードを含むコミュニティカード配布ロジックを純粋関数として実装する
  - フェーズに応じてデッキから1枚をバーン（破棄）した後にコミュニティカードを配布する純粋関数を新規作成する
  - フロップ: バーン1枚 + 配布3枚（デッキから計4枚消費）
  - ターン: バーン1枚 + 配布1枚（デッキから計2枚消費）
  - リバー: バーン1枚 + 配布1枚（デッキから計2枚消費）
  - 元のデッキ配列を変更せず、新しい配列を返すことを保証する
  - 各フェーズでデッキ残枚数とコミュニティカード枚数が正しいことを単体テストで検証する
  - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - _Contracts: dealCommunityCards Service_

- [ ] 4.2 フェーズ遷移処理のカード配布を新しい純粋関数に委譲する
  - フェーズ遷移時のコミュニティカード配布ロジックを、タスク4.1で作成した純粋関数の呼び出しに置き換える
  - フェーズ遷移、プレイヤーリセット、次のアクティブプレイヤー計算などの既存責務は維持する
  - _Requirements: 4.1, 4.2, 4.3, 4.4_

- [ ] 5. 全バグ修正のリグレッション確認
  - 既存の単体テストが全てパスすることを確認する
  - 既存のPlaywright E2Eテストが全てパスし、スクリーンショット差分が0であることを確認する
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3, 4.4_
